name: Test Minimal Reusable
on:
  workflow_call:
    inputs:
      msg:
        type: string
        default: "hello"
jobs:
  echo:
    runs-on: lux-build-linux-amd64
    steps:
      - run: echo "${{ inputs.msg }}"
